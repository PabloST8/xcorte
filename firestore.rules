rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DESENVOLVIMENTO: Permite tudo sem autenticação
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
